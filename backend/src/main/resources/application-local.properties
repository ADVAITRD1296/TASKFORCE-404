# Local Development Override
spring.datasource.url=jdbc:mysql://localhost:3306/bookzy_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Server
server.port=8080

# Disable Firebase for local dev (no config file needed)
firebase.config.path=
